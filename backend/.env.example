# 后端配置示例：复制为 backend/.env 后按需修改（.env 已被 .gitignore 忽略，不会入库）

# 监听地址：0.0.0.0 允许局域网访问；仅本机调试可改为 127.0.0.1
BACKEND_HOST=0.0.0.0

# 监听端口：需与 frontend/.env 的 VITE_BACKEND_ORIGIN 保持一致
BACKEND_PORT=8000

# 允许的跨域来源，多个用英文逗号分隔；* 表示全部放开（仅限本地开发）
CORS_ORIGINS=*
