# ============================================================
# 数字孪生工厂 - 前端环境配置示例
# 复制为 .env 后按需修改；.env 不入库，仅本机生效
# 所有变量必须以 VITE_ 开头才会暴露给浏览器代码
# 与 backend/.env.example 使用同一套口径（端口/路径）
# ============================================================

# 浏览器访问后端的 REST 基础路径与 WebSocket 路径。
# 本地开发走 Vite dev server 代理（见 vite.config.ts），
# 因此保持相对路径即可，不在代码里写死后端地址。
VITE_API_BASE=/api
VITE_WS_PATH=/ws

# 代理目标：后端实际监听地址，需与 backend/.env 的
# BACKEND_HOST / BACKEND_PORT 一致
VITE_BACKEND_HOST=127.0.0.1
VITE_BACKEND_PORT=8000

# Vite dev server 监听端口（端口被占用会直接报错退出，见 scripts/dev-precheck.mjs）
DEV_SERVER_PORT=3000
